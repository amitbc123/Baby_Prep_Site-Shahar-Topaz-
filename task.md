# Auto-Update Spec (moved)

This was a near-duplicate of `docs/specs/02-auto-update.md` — the in-app auto-update spec,
now fully implemented (`:core:update` + `:feature:update`, see `PROGRESS.md`'s Phase 2
entry). See `docs/specs/02-auto-update.md` for the canonical (also trimmed) version; full
original text for both is in git history.
