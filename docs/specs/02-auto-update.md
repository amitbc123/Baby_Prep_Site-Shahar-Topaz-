# Auto-Update — Spec (implemented)

Original spec for the in-app updater. Fully implemented as `:core:update` +
`:feature:update` — see `PROGRESS.md`'s Phase 2 entry for what was built (semver comparison,
async non-blocking startup check, download with SHA-256 verification, `PackageInstaller`
install flow, mandatory-update support, manual "Check for updates" in Settings).

Full original spec text preserved in git history:
`git log --follow -- docs/specs/02-auto-update.md`.
